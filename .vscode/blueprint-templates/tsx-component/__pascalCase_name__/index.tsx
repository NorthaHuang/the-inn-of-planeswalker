import React from 'react';
import StyledWrapper from './styled';

const {{pascalCase name}} = (): JSX.Element => {
  return (
    <StyledWrapper>
			<p>{{ pascalCase name }}</p>
    </StyledWrapper>
  );
};

export default {{pascalCase name}};
