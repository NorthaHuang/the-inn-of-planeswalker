import React from 'react';
import StyledWrapper from './styled';

const Decks = (): JSX.Element => {
  return (
    <StyledWrapper>
			<p>Decks</p>
    </StyledWrapper>
  );
};

export default Decks;
